export default class Sphere {

    /**
     * Represents a SphereEngine Compiler
     * @constructor
     * @param 
     */
    constructor (options) {
        this.compilerToken = options.token
    }

    /**
     * Requests for a Sphere Compile
     * @param {object} options
     */
    compile (options = { language: 'python', source: '' }) {

    }

}
